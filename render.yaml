# Render Deployment Configuration

# The configuration for your Render service
services:
  - type: web
    name: my-app
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    runtime: node
    region: oregon
